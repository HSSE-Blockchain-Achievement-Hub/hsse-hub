# https://web3py.readthedocs.io/en/stable/web3.contract.html

# Конструируем контракт ".constructor().transact()"
# wait_for_transaction_receipt(tx_hash)

# test_contract = w3.eth.contract(
#     address=tx_receipt.contractAddress,
#     abi=abi
# )